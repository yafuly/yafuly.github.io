# Yafu Li Personal Website

This repository contains the source for <https://yafuly.github.io>, built with the Academic Pages Jekyll template.

## Local Preview

```bash
bundle install
bundle exec jekyll serve -l -H localhost
```

Then open <http://localhost:4000>.
